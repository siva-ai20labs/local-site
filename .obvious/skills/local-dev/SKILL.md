# Local Dev Onboarding — siva-ai20labs/test

> **Status: PLACEHOLDER** — No application code exists in this repository yet.
> All fields below are `TODO(confirm):` stubs. Do not follow these instructions until
> they have been verified by a full Autobuild setup run.

## Prerequisites

`TODO(confirm):` list required runtime(s), e.g. Node.js version, Python version, Go version.

## Install Dependencies

```sh
# TODO(confirm): replace with the actual install command
# e.g. npm install / pip install -r requirements.txt / go mod download
```

## Start the Application

```sh
# TODO(confirm): replace with the actual start command
# e.g. npm run dev / python -m uvicorn app:main --reload / go run ./cmd/server
```

## Ports

`TODO(confirm):` list the port(s) the application listens on (e.g. `3000`, `8080`).

## Environment Variables

`TODO(confirm):` list required env vars and where to obtain their values.
Example format once known:

| Variable | Description | Example |
|----------|-------------|----------|
| `TODO` | TODO | TODO |

## Primary User Flow

`TODO(confirm):` describe the primary user flow for manual smoke-testing once the app is running.

## Notes

This skill file was created as part of the minimal greenfield Autobuild install on
2026-06-24. Re-run the full 8-phase Autobuild setup after adding application code to
replace this stub with verified onboarding instructions.

